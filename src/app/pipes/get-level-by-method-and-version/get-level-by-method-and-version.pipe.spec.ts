import { GetLevelByMethodAndVersionPipe } from './get-level-by-method-and-version.pipe';

describe('GetLevelByMethodAndVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLevelByMethodAndVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
