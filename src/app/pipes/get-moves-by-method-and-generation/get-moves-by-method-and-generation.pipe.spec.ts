import {GetMovesByMethodAndGenerationPipe} from './get-moves-by-method-and-generation.pipe';

describe('GetMovesByMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMovesByMethodAndGenerationPipe();
    expect(pipe).toBeTruthy();
  });
});
